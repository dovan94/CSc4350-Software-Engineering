export class User {
    user_id: string;
    username: string;
    email: string;
    password: string;
    first_name: string;
    last_name: string;

    constructor(username, email, password, first_name, last_name) {}
}
