export class User {
    id: number;
    username: string;
    email: string;
    password: string;
    first_name: string;
    last_name: string;

    constructor(username, email, password, first_name, last_name) {}
}
